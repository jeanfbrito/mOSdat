---
date: "2026-05-16"
project: mosdat
topic: 'Root causes:'
kind: war-story
scope: project-shared
confidence: medium
---

- `yaml.safe_load` passed.
   - Pydantic schema validation failures in all 5 new scenarios:
     - Pre-flight tester found all 5 fail Pydantic validation.
     - Root causes:
       1. `click: true` invalid; `ClickStep.click` accepts only `"left"` or `"right"`.
       2. Bare `verify_not:` invalid; `VerifyStep` requires positive `verify:`.
     - Fix:
       - Patched 24 `click: true` → `click: left`.
       - Added positive `verify:` twin to 6 bare `verify_not:` steps.
